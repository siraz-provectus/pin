$("#pin_<%= @pin.id %>").replaceWith("<%= escape_javascript(render(
  'comments/comments', 
  pin: @pin
)) %>");
$("#comment_<%= @pin.id %>").replaceWith("<%= escape_javascript(render(
  'comments/comment_form'
)) %>");