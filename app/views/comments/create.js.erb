$( "#pin_<%= @comment.pin_id %> .comments_container form" ).before("<%= escape_javascript(render(
  'comments/comment', com: @comment
)) %>");

$( "#pin_<%= @comment.pin_id %> .comments_container form textarea" ).val('');